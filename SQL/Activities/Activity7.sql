select sum(purchase_amount) as totalamnt from orders;
select avg(purchase_amount) as avgamnt from orders;
select min(purchase_amount) as minamnt from orders;
select max(purchase_amount) as maxamnt from orders;
